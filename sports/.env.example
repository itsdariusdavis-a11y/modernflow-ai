# Optional: override the MLB Stats API base URL (e.g. point at your own proxy).
# Leave unset to use the Vite dev proxy in development and the public API in prod.
# VITE_STATSAPI_BASE=https://statsapi.mlb.com
