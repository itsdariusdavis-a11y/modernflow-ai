# ModernFlow AI — credential contract (copy to .env, never commit .env)
# Verified during B.L.A.S.T. Phase L; results logged in /memory/progress.md.

# Contact form → GoHighLevel inbound webhook (server/routers.ts)
GHL_WEBHOOK_URL=https://services.leadconnectorhq.com/hooks/YOUR_WEBHOOK_ID

# MySQL / TiDB connection (Drizzle ORM)
DATABASE_URL=

# Auth token signing (auto-injected by Manus if hosted there)
JWT_SECRET=

# --- Additional credentials will be added here after Blueprint Q2 (Integrations) ---
